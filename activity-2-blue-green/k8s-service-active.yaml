apiVersion: v1
kind: Service
metadata:
  name: bg-app-service-active
spec:
  type: NodePort
  selector:
    app: bg-app
    color: ACTIVE_COLOR
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
    nodePort: 30081
